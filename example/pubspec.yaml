name: drift_libsql_example
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  drift_libsql:
    path: ../
  flutter_bloc: ^8.1.6
  provider: ^6.1.2
  path_provider: ^2.1.4
  drift: ^2.20.0
  drift_flutter: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  drift_dev: ^2.20.1

flutter:
  uses-material-design: true
